## Write JavaScript in Pig Latin, because *you* can

### Here's how it works:

**Per Object Usage**

    sjay(Math);
    sjay(Object);
    sjay(String);
    sjay(RegExp);


**All The JavaScript available**  

    avaScriptJay();
